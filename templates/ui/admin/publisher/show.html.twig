{% extends 'ui/admin/layout/admin_layout.html.twig' %}

{% block title %}Publisher{% endblock %}

{% block body %}
    <h1>Publisher</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publisher.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ publisher.title }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ publisher.address }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ publisher.createdAt ? publisher.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ publisher.updatedAt ? publisher.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ publisher.deletedAt ? publisher.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-secondary" href="{{ path('ui_admin_publisher.index') }}">Back to list</a>

    <a class="btn btn-warning" href="{{ path('ui_admin_publisher.edit', {'id': publisher.id}) }}">Edit</a>

    {{ include('ui/admin/publisher/_delete_form.html.twig') }}
{% endblock %}
